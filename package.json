{
  "name": "google-meet-join-on-time",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "start": "mkdir -p dist && cp src/manifest.json dist/manifest.json && too -c='tsc -w' -c='sass -w ./src/context/:./dist/context/'",
    "build": "pnpm build:js && pnpm build:css",
    "build:js": "tsc && cp src/manifest.json dist/manifest.json",
    "build:css": "sass src/context/:dist/context/",
    "lint": "eslint ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Google Meet",
    "Automation",
    "Join on time",
    "now"
  ],
  "author": "otiai10",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.10.0",
    "eslint": "^9.10.0",
    "globals": "^15.9.0",
    "sass": "^1.78.0",
    "too": "^2.4.0",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.5.0"
  },
  "dependencies": {
    "@types/chrome": "^0.0.270"
  }
}
